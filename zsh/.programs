pacstrap -K /mnt base linux linux-firmware iwd vi man-db man-pages texinfo \
grub efibootmgr intel-ucode sudo xorg pipewire wireplumber pipewire-audio \
pipewire-alsa pipewire-pulse sof-firmware zsh zsh-completions bolt stow \
unzip xclip btop fzf openssh git base-devel gtk3 libcups libxss nss \
ttf-liberation xdg-utils curl xorg-xinit wget lib32-systemd vulkan-icd-loader \
lib32-vulkan-icd-loader vulkan-tools steam ttf-dejavu maim xbindkeys python-pip \
ruby imlib2 aarch64-linux-gnu-gcc aarch64-linux-gnu-gdb bc cpio rsync mono \
dotnet-sdk dotnet-runtime dotnet-runtime-6.0 gtk-sharp-2 cscope nodejs-lts-hydrogen npm

npm install neovim

INTEL GRAPHICS:
pacman -S xf86-video-intel mesa lib32-mesa vulkan-intel

NVIDIA GRAPHICS:
pacman -S nvidia nvidia-utils lib32-nvidia-utils nvidia-settings

mkdir -p dev/aur && \
cd dev/aur && \
git clone https://github.com/marcinwilkdev/dwm && \
git clone https://github.com/marcinwilkdev/st && \
git clone https://github.com/marcinwilkdev/slstatus && \
git clone https://aur.archlinux.org/dmenu-git.git && \
git clone https://aur.archlinux.org/google-chrome.git && \
git clone https://aur.archlinux.org/slock-git.git && \

makepkg && sudo pacman -U <package>.tar.zst // in all of them

{
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
  cargo install fd-find ripgrep exa bat git-delta bob-nvim gitui du-dust
  bob install stable
  bob use stable
}

sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
