1. Preparation
- download iso from https://archlinux.org/download/
- dd if=<arch_img> of=/dev/sda bs=1M
- boot from UEFI arch pendrive option.

2. Pre chroot:
setfont ter-132b && \
iwctl && \
timedatectl && \
cfdisk (1GB boot, 1GB swap, rest linux partition)

ROOT_PATH=<root> && \
SWAP_PATH=<swap> && \
BOOT_PATH=<boot> && \
mkfs.btrfs $ROOT_PATH && mkswap $SWAP_PATH && mkfs.fat -F 32 $BOOT_PATH && \
mount $ROOT_PATH /mnt && mount $BOOT_PATH /mnt/boot && swapon $SWAP_PATH && \
reflector > /etc/pacmand.d/mirrorlist && \
pacstrap -K /mnt base linux linux-firmware iwd vi man-db man-pages texinfo <all_programs> && \
genfstab -U /mnt >> /mnt/etc/fstab && \
arch-chroot /mnt

3. Post chroot:
ln -sf /usr/share/zoneinfo/Europe/Warsaw /etc/localtime && \
hwclock --systohc && \
- uncomment en_US.UTF-8 in /etc/locale.gen 
locale-gen && \
echo "LANG=en_US.UTF-8" > /etc/locale.conf && \
echo "marcin-arch" > /etc/hostname && \
passwd

4. Setup GRUB:
grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=GRUB && \
grub-mkconfig -o /boot/grub/grub.cfg && \
exit && \
umount -R /mnt && \
reboot

5. First boot:
useradd -m marcin && \
passwd marcin && \
usermod -a -G wheel marcin

6. Relog into marcin:
- uncomment VerbosePkgLists and ParallelDownloads in /etc/pacman.conf
- chsh -s /usr/bin/zsh

7. Keyboard config:
/etc/X11/xorg.conf.d/00-keyboard.conf
Section "InputClass"
	Identifier "system-keyboard"
	MatchIsKeyboard "on"
	Option "XkbLayout" "pl,us"
	Option "XkbOptions" "caps:swapescape"
EndSection

8. Touchpad config:
/etc/X11/xorg.conf.d/30-touchpad.conf
Section "InputClass"
	Identifier "touchpad"
	Driver "libinput"
	Option "NaturalScrolling" "true"
	Option "ClickMethod" "clickfinger"
EndSection

9. Thunderbolt dock config:
boltctl

10. Sound setup:
sudo systemctl --user --now enable pipewire pipewire-pulse wireplumber && \
wpctl status
wpctl set-default <id>

11. Display setup:
xrandr --output eDP-1 --off --output DP-1-2 --primary --auto --output DP-1-1 --auto --right-of DP-1-2
